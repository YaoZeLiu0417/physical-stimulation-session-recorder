# Web UI
streamlit==1.37.1
streamlit-webrtc==0.47.1

# WebRTC / 视频
aiortc==1.6.0          # 需搭配 av < 12
av==11.0.0             # 与 aiortc 1.6.0 兼容，Linux 有预编译轮子

# 基础依赖
numpy>=1.24,<2.0
requests>=2.31,<3
protobuf<5             # 与当前 streamlit 版本更稳
python-dotenv>=1.0,<2  
